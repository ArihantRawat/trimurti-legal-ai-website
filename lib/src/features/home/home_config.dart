class HomeConfig {
  // Set this to true to restore the longer site with Why Trimurti,
  // extra product direction sections, and the process timeline.
  static const bool showDetailedSections = false;
}
